# Bu workflow disable edilmistir.
# Deployment artik deploy.yml uzerinden yapiliyor.
name: Disabled Jekyll Workflow

on:
  workflow_dispatch:  # Sadece manuel olarak tetiklenebilir, otomatik calismaz

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No operation
        run: echo "Jekyll workflow disabled. Using deploy.yml instead."
